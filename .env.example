# PostgreSQL Database Configuration
# Copy this file to .env and update with your actual values

POSTGRES_DB=anok_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# Backend API Configuration
BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=dev
JWT_SECRET=izVcTEc8OoHK65UmwVj6FOYq5W/KghPmQ1Nqkxkj5Fkwk/24DhyGbcD5RdAOMPIp
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# AWS Configuration
# For local development with SSO: just set AWS_PROFILE
# The app will automatically use SSO credentials from ~/.aws/config
AWS_PROFILE=default

# AWS Credentials (only needed for non-SSO environments like Render)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# S3 Configuration
S3_BUCKET=your-bucket-name-from-cdk-output
AWS_REGION=us-east-2
S3_PREFIX=uploads/
S3_PRESIGN_EXP_MINUTES=15

# CloudFront Configuration
# Set CLOUDFRONT_DOMAIN after deploying CDK stack (e.g., d1234567890.cloudfront.net)
CLOUDFRONT_DOMAIN=your-cloudfront-domain-from-cdk-output
CLOUDFRONT_ENABLED=false
