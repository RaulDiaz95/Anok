{
  "name": "anok",
  "version": "1.0.0",
  "description": "Anok Platform - Monorepo (Spring Boot API + Frontend)",
  "private": true,
  "scripts": {
    "dev": "docker compose up --build -d && cd webapp && pnpm dev",
    "dev:skip-aws": "docker compose up --build -d && cd webapp && pnpm dev",
    "stop": "docker compose down",
    "clean": "docker compose down -v",
    "install:webapp": "cd webapp && pnpm install",
    "build:webapp": "cd webapp && pnpm build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}