{
  "name": "anok",
  "version": "1.0.0",
  "description": "Anok Platform - Monorepo (Spring Boot API + Frontend)",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d && sleep 3 && concurrently \"cd backend && ./mvnw spring-boot:run\" \"cd webapp && pnpm dev\"",
    "clean": "docker compose down -v && cd backend && ./mvnw clean",
    "install:webapp": "cd webapp && pnpm install",
    "build:webapp": "cd webapp && pnpm build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}